import 'package:flutter/material.dart';

class ArchefTask extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text(
          'Archef tasks',
          style: TextStyle(fontSize: 40),
        ),
      ),
    );
  }
}
